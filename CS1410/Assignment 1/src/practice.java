public class practice {
    public static void main(String[] args) {
        double length = 5.0;
        double height = 2.0;
        double area = length * height;

        System.out.println("The area for the rectangle (" + length + " by " + height + ") is: " + area);
    }
}